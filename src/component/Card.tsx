import { Component, ReactNode } from "react"

interface Props{
    caption: string;
    description: string;
    image: string;
}

export default class Cards extends Component {
    render(): ReactNode {
        return ;
    }
}